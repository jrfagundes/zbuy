virtual-store-dir-max-length=25
# node_modules plano (sem symlinks .pnpm). Necessário para os builds nativos
# do React Native/Expo (CMake/ninja do reanimated entrava em loop com os
# symlinks do pnpm). Recomendação oficial do Expo para monorepos pnpm.
node-linker=hoisted
